# media-backup-client 0.4.15

- Android 与 iOS 的备份偏好及媒体筛选文案按当前行为统一；iOS 偏好变化时更新待执行的后台任务。
- iOS 对异常时间线及本地批次结构显示可处理的错误，避免强制类型转换导致崩溃；HTTP 非成功响应不向界面或持久任务暴露原始服务端正文。
- 依赖精确锁定 Foundation Client 0.9.16。发行号更新为 0.4.15；Rust 移动状态合同仍为 0.4.0，既有队列格式和 ABI v2 不变。

## 验证与交付

- Rust 工作区测试、严格 Clippy、移动 ABI 合同检查和 iOS IPA 打包脚本测试通过。
- Release 工作流提供签名 Android APK 和未签名 iOS IPA；IPA 安装前需使用自己的 Apple 身份签名。
